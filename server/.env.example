# Copia questo file in ".env" (nella stessa cartella "server")
# e adatta i valori alla tua installazione MySQL.
# ".env" NON va mai caricato su GitHub (mettilo nel .gitignore).

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=f1

# Porta su cui parte il server Express
PORT=3000
